[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"
[project]
name="cynthia"
version="0.0.0"
[project.scripts]
cynthia="cynthia.__main__:main"
[tool.setuptools]
packages = ["bot", "context", "utils"]

